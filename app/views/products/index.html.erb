<h1>All Products</h1>
<%= link_to "Add Product",new_product_path %>
<%@products.each do |product| %>
    <li><%= image_tag(product.url)%> <%=product.name%> | Price: <%=product.price%>
    <p><%=link_to "View Product",product_path(product)%></p></li>
<% end %>
